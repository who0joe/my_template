# you can write to stdout for debugging purposes, e.g.
# print("this is a debug message")

def solution(B):
    # Make String to int # = 1 . = 0 and store to new_raw_array

    # sum side cell and make it into other array with same structure

    # count cell 3 count of it
    # count 2 with no sidecell of 3 and devide by 2
    # count 1 

    
    print(f"R{},")


# you can write to stdout for debugging purposes, e.g.
# print("this is a debug message")

def solution(U, L, C):
    # write your code in Python 3.6
    # if sum of all C 
    if np.sum(C) != U+L:
        return print("impossible")
        
    # fill if c in 2 to same structure 

    # fill 0(upper) until given value(U)

    # fill 1(lower) rest of it.

    pass


# you can write to stdout for debugging purposes, e.g.
# print("this is a debug message")

def solution(A, S):
    # write your code in Python 3.6
    # sort A to asc
    # first, find one element matches S - record of not 
    # find two elements sum of two values matches S*2
    # -> hmm....
    # 


    pass


# you can write to stdout for debugging purposes, e.g.
# print("this is a debug message")

def solution(S):
    # loop until ? is not exist : loop = counts of ?
    # array append and 
    # check first constrains where two letters attached -> fill with other letter frist and relace it
    # if ab? or ?ab then 

    
    return S

